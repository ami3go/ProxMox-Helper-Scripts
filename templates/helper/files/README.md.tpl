# Files

Store static payload files copied to the Proxmox host, container, or VM by __HELPER_NAME__ here.

Resolve this directory relative to `install.sh`; do not assume a particular working directory.
