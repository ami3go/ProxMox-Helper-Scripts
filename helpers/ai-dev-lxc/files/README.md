# Files

Store static payload files copied to the Proxmox host, container, or VM here.

The installer should resolve this directory relative to its own location and must not assume the repository working directory.
